## Why

用户从物品详情删除记录后返回首页，首页列表和“物”标签数量仍显示删除前的数据。详情页删除成功后没有通知前一页刷新，导致首页 `onShow` 不会重新加载。

## What Changes

- 删除详情记录成功后，在返回前标记前一页需要刷新。
- 首页恢复显示时重新获取当前列表和全部标签数量。
- 增加删除返回首页的回归测试。

## Impact

- 前端：`miniprogram/pages/detail/index.js`。
- 校验：新增删除刷新测试并接入 `scripts/ci-build.sh`。
- 云函数、数据结构和接口不变。
